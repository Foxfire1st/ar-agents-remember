# mcp/src/agents_remember/mcp/tools/knowledge.py

| Field | Value |
| --- | --- |
| repository | agents-remember |
| path | `mcp/src/agents_remember/mcp/tools/knowledge.py` |
| doc_type | `file-level-onboarding` |
| lastUpdated | 2026-09-18T15:30+02:00 |
| lastVerifiedCommitHash | `5d64af264dc89b51d5c5e6454216573abde3c12e` |
| lastVerifiedCommitDate | 2026-09-20T02:50:22+02:00|
| reviewedWorkingCandidate | candidate `ar/260915-ks-l32-ar`, uncommitted; base `7dcec036094768c5f50e571fb45e59a27ae78efc` |
| governingOverview | `mcp/src/agents_remember/mcp/tools/overview.md` |

## Governing Overview

[mcp/tools route overview](overview.md)

## Purpose

The payload builders for the five mounted `knowledge_*` operation families — one builder per operation, each of which "validates its wire request, delegates to the application seam that owns the operation, and returns the typed shape the response model declares". Its docstring states the property that defines the whole module: "**Nothing here decides anything**: no classification is computed, no effect label is inferred, no draft is authored, no rationale is judged, no ambiguity is resolved by choosing, no missing assessment is filled, and no compatibility verdict is produced. Where a handler would have to decide something, it returns the unresolved state instead." **It deliberately has no renderer of its own, no detection logic, no diff algorithm and no projection writer**: each of those belongs to a seam it calls, and the two requirement-level cases its docstring singles out are why it stays short — `knowledge_read` "returns recorded claims and assessments as attributed records" and the payload it returns "is the view payload itself, so the classification rule has exactly one implementation", while `knowledge_diff` includes "semantic effect labels ... only when supplied by an identified agent/assessment, not inferred from the diff".

## Code Commentary

### Logic

**Five builders, one per mounted operation family, and every one of them delegates rather than implements.** `knowledge_read_payload` validates the view name against the five named views, resolves the read context through `open_read_context` and returns what `read_knowledge_view` produced; `knowledge_change_payload` records through the admitted write operation for its kind; `knowledge_diff_payload` calls `diff_knowledge_scope`; `knowledge_integrity_check_payload` reads recorded detection conditions; `knowledge_project_payload` builds a `DestinationProfile` and calls `project_knowledge`. No builder contains a comparison, a classification, a rendering or a filesystem write, so the diff engine, the view renderer and the projection writer each keep exactly one implementation.

**Where a handler would have to decide, it returns the unresolved state instead.** `knowledge_read_payload` refuses a view outside `source_context`, `invariant`, `family`, `review_matrix` and `curation_queue` through `_refused_read(view, repositoryId, "unknown_view", ...)` rather than picking a default view; a read the seam refused is returned as its typed refusal rather than as an empty page. `knowledge_integrity_check_payload` returns `"compatible": None` **present** beside `"unresolved": conditions["unresolved"]`, which is the difference between reporting conditions and manufacturing a verdict from a passing test — computing `true` from a detection's silence would be the semantic conclusion this leaf forbids. `_no_detection_run(scope_id)` is the honest special case of the same rule: no recorded run yields no conditions, a stated limitation and a named unresolved item, never an empty-list success.

**The declared write kinds name what the surface may be asked for, and every one of them is refused by name.** `DECLARED_CHANGE_KINDS` is `("evidence_claim", "verification_observation", "invariant_revision", "assumption", "semantic_change_set", "requirement_revision")` — the record kinds this mounted surface is asked about — and the module comment records why the vocabulary is *declared* rather than *admitted*: "The earlier spelling advertised two 'admitted' kinds and then refused both anyway, which made the tool's own description false and told the caller to supply an input its published signature cannot carry. Both are removed: the set below is what the surface may be *asked* for, the refusal is unconditional for every member of it, and the reason names where the write actually happens." `knowledge_change_payload` therefore has exactly **one** branch and it refuses every kind — declared here or never heard of — with the shipped `registration_absent` code and a detail naming the entry point that owns the write: `WRITE_ENTRY_POINT`, the `agents-remember knowledge-ingest` subcommand, "which commits a whole curator hand-off list through the admitted batch". The reason is deliberately the same for both cases, because "two spellings of 'this tool does not write' would suggest the first one might". This module never invents a write seam for a kind another leaf owns, and it reports no missing destination facts for a write that does not exist here.

**`knowledge_read` returns the view payload itself, which is what keeps the classification rule single-sourced.** `_view_request(request)` rebuilds one `ViewRequest` from the flat tool arguments — including `source_path`, the optional path seed the published signature carries — and reconstructs a continuation token opaquely — its docstring is explicit that "its snapshot binding is checked by the seam against the snapshot the dataset actually declares, so this builder never guesses one" — and the success branch returns `"payload": payload.model_dump(mode="json")` beside the echoed `view`, `snapshot` and `completeWithinDeclaredScope`. No reshape, no re-render and no second classification path exists here; requirement 6.8's "not a second shape" holds because there is no second shape to violate it. The four request value objects `ReadToolRequest`, `ChangeToolRequest`, `DiffToolRequest` and `ProjectToolRequest` exist so the builders are one argument wide while the published FastMCP signature stays flat.

**`knowledge_diff` carries only labels an identified agent or assessment supplied.** `_supplied_effect_labels(request)` reads the caller's own body, accepts both the snake-case and camel-case spelling of the list, and appends an entry only when it is a dict carrying `supplied_by` — an unlabelled diff therefore returns `"semanticEffectLabels": []` rather than a derived guess. `_diff_request(repository_id, request)` then validates the shipped `KnowledgeDiffRequest` from that same caller body with the namespace injected, and a refused comparison returns its refusal fields with no payload, so "the label was not supplied" and "the comparison was refused" stay distinguishable states.

**The integrity check reports recorded conditions and their observable limits, and produces no verdict.** `_recorded_conditions(database_path, repository_id, scope_id)` opens the dataset read-only, selects the namespace's recorded `detection_run` rows in record-id order and closes the connection in a `finally`; with no rows it returns `_no_detection_run`. With rows it hands them to `_run_for_scope`, because **the requested scope SELECTS the run instead of being echoed beside one**: the first recorded run is the answer only when the caller named no scope, and when one is named a run measured over a different scope is not a weaker answer but the wrong one, so the search continues over `run.governing_route_id` and a namespace with no matching run is told so by `_no_run_for_scope` — a stated limitation ("no recorded detection run measured the requested scope {scope_id!r}; the conditions of another scope's run are not reported in its place") and a named unresolved item, never another run's conditions under the requested scope's name. With a run selected it builds an `OpenedKnowledgeStore` over the same connection and reads through `read_detection_run`, and `_condition_report(result, scope_id)` maps the result's signals to one `{"code": signal.condition, "matched_facts": [signal.detail]}` entry each, carries `result.run.limitations` through verbatim, and sets `unresolved` to `["no condition matched the recorded scope"]` when nothing matched. A run that could not be read yields `"the recorded detection run could not be read"` as its limitation and the refusal detail as its unresolved item — again a report and not a status.

**The project builder names views and identities, and refuses before it renders.** `_projection_requests(repository_id, views)` turns each caller-named input into one `(ViewRequest, subject)` pair, defaulting the subject to the view name, because the identity is what the artifact records and the manifest tracks. An empty request set is refused by `_refused_project(destinationRoot, "unresolved_projection_input", "no view was named to project, so no artifact is emitted")` before any writer is constructed, and a refused report is relayed through the same helper, so both refusal routes carry the destination root and the renderer version. The success branch reports per-path outcomes — `published` for outcomes in `published` or `unchanged`, plus `retained` and `discrepancies` verbatim — and returns the data; the file writing happened inside the projection writer, not here.

### Conventions

Request shapes are frozen dataclasses rather than loose dictionaries, so a builder's argument list is one value wide while the published tool signature stays flat and FastMCP keeps deriving the wire schema from it. Bounded vocabularies are module constants that the refusal messages quote: `DECLARED_CHANGE_KINDS` is declared once and refused wholesale by the builder, `WRITE_ENTRY_POINT` is "kept as a constant because two detail strings and a test quote it", and the five admitted view names appear as the literal tuple the read builder checks against. Where a vocabulary belongs to another module it is imported rather than restated — `ProjectionOptions` and `project_knowledge` from the projection operation, `VIEW_RENDERER_VERSION` and `read_knowledge_view` from the views seam, `diff_knowledge_scope` from the diff seam, `DestinationProfile` from the projection-manifest vocabulary, `KnowledgeDiffRequest` and `ViewRequest`/`ViewContinuation` from the models — so this module adds no sixth spelling of a name another leaf owns. `__all__` exports exactly the four request dataclasses and the five builder functions, keeping `_refused_read`, `_view_request`, `_diff_request`, `_supplied_effect_labels`, `_recorded_conditions`, `_run_for_scope`, `_no_run_for_scope`, `_no_detection_run`, `_condition_report`, `_projection_requests` and `_refused_project` module-local. Every private helper that can fail does so by returning a state the caller can read — a `refused` dict, an unresolved list, an empty label list — rather than by raising, so a refusal crosses the wire as a first-class result.

### Invariants And Boundaries

- **No builder decides anything.** No classification is computed, no effect label inferred, no draft authored, no rationale judged, no ambiguity resolved by choosing, no missing assessment filled and no compatibility verdict produced anywhere in the module.
- **A kind without an admitted write operation is refused as `registration_absent`.** `DECLARED_CHANGE_KINDS` holds exactly `evidence_claim`, `verification_observation`, `invariant_revision`, `assumption`, `semantic_change_set` and `requirement_revision`, and `knowledge_change_payload` has one unconditional refusal branch that returns that code with a detail naming what is missing — the entry point that can write, `WRITE_ENTRY_POINT`.
- **`compatible` is `None` and present, never computed.** `knowledge_integrity_check_payload` reports conditions, traversal scope, limitations and an explicit `unresolved` list, and derives no status from the absence of a matched condition.
- **The read payload is the view payload.** The success branch dumps the seam's own payload; no second renderer, filter or classification exists in this module for requirement 6.8 to be violated by.
- **A continuation is carried opaquely.** `_view_request` mints a token whose snapshot binding the seam re-checks; the builder never resolves or guesses a snapshot binding.
- **Effect labels are only what an identified source supplied.** `_supplied_effect_labels` requires a `supplied_by` on each accepted entry and returns an empty list otherwise; nothing is inferred from the change.
- **An empty projection request set writes nothing.** `knowledge_project_payload` returns the `unresolved_projection_input` refusal before the writer is constructed.
- **This module opens no write path of its own.** It reads through the shipped read-only connection and writes only by delegating to the projection writer; `_recorded_conditions` closes its connection in a `finally`.
- **An unreadable recorded run is reported, not defaulted.** `_condition_report`'s refusal branch returns a stated limitation and an unresolved item instead of an empty success.
- **The requested scope selects the detection run; it is never just echoed.** `_run_for_scope` matches the caller's scope against the registered traversal scope the run itself records (`governing_route_id`) and keeps searching past a run measured over another one; a caller naming no scope still gets the first recorded run, and a caller whose scope matches no run gets `_no_run_for_scope`'s limitation and unresolved item rather than a different run's conditions reported under the requested scope's name.

## Docs References

No domain documentation source is configured for this repository (`system/sources.md` carries no `Domain Documentation` entries). The statements below are grounded in repository source only.

| Finding | Anchor | Source |
| --- | --- | --- |
| No configured domain documentation could be checked. | — | — |

## Repo-Internal References

| Finding | Anchor | Source |
| --- | --- | --- |
| The module's own statement that nothing here decides anything, the list of decisions it refuses to make, and the unresolved-state rule that replaces them. | "no classification is computed, no effect label is inferred, no draft is authored"; "unresolved state instead" | mcp/src/agents_remember/mcp/tools/knowledge.py:1-18 |
| The public surface: the four request value objects and the five payload builders, one per mounted operation family. | `__all__` | mcp/src/agents_remember/mcp/tools/knowledge.py:67-77 |
| The declared set of record kinds this surface may be *asked for*, with the reason a kind outside it is refused rather than written by a second path -- and note the refusal is unconditional for every member, because this surface has no admitted write operation for any kind. | `DECLARED_CHANGE_KINDS` | mcp/src/agents_remember/mcp/tools/knowledge.py:92-99 |
| The flat read request as one frozen value, with the continuation, the two revision selectors and the optional source roots it carries. | `ReadToolRequest` | mcp/src/agents_remember/mcp/tools/knowledge.py:62-80 |
| The change, diff and project request values, including the per-path overwrite authorization the projection operation consumes. | `ChangeToolRequest`; `DiffToolRequest`; `ProjectToolRequest` | mcp/src/agents_remember/mcp/tools/knowledge.py:118-127; mcp/src/agents_remember/mcp/tools/knowledge.py:128-138; mcp/src/agents_remember/mcp/tools/knowledge.py:139-149 |
| The one refusal envelope the read operation returns for an unknown view and for a view the seam refused. | `_refused_read` | mcp/src/agents_remember/mcp/tools/knowledge.py:162-170 |
| The read builder: the five-view closure, the resolver call, the seam call, and the payload returned as the view payload itself. | `knowledge_read_payload`; "unknown_view"; "not one of the five named query views" | mcp/src/agents_remember/mcp/tools/knowledge.py:194-260 |
| The validated view request with its continuation reconstructed opaquely, its snapshot binding left to the seam. | `_view_request`; `ViewContinuation` | mcp/src/agents_remember/mcp/tools/knowledge.py:175-195; mcp/src/agents_remember/models/knowledge/view.py:370-370; mcp/src/agents_remember/models/knowledge/view.py:370-381 |
| The record builder and its one `registration_absent` refusal, deliberately the same reason for a declared kind and for one this surface has never heard of -- the surface has nothing to add in either case, so the refusal names the entry point that owns the write. | `knowledge_change_payload`; "registration_absent" | mcp/src/agents_remember/mcp/tools/knowledge.py:287-315 |
| The comparison builder, which validates its request from the caller's own body with the namespace injected, relays the shipped diff result, and carries only the labels that body supplied. | `knowledge_diff_payload`; `_diff_request` | mcp/src/agents_remember/mcp/tools/knowledge.py:321-373; mcp/src/agents_remember/mcp/tools/knowledge.py:375-389 |
| The label filter that keeps only entries an identified agent or assessment supplied, quoting the requirement that they are "inferred from the diff" only when supplied. | `_supplied_effect_labels`; "supplied_by"; "inferred from the diff" | mcp/src/agents_remember/mcp/tools/knowledge.py:391-405; mcp/src/agents_remember/mcp/registration/knowledge.py:139-139; mcp/src/agents_remember/mcp/tools/knowledge.py:16-16; mcp/src/agents_remember/models/tools/knowledge_responses.py:73-73; mcp/src/agents_remember/certification/lifecycle_admission.py:252-252 |
| The integrity builder that returns `compatible: None` present beside its explicit unresolved list, and the recorded-conditions read with its connection closed in a `finally`. | `knowledge_integrity_check_payload`; `_recorded_conditions`; "compatible" | mcp/src/agents_remember/mcp/tools/knowledge.py:405-443; mcp/src/agents_remember/mcp/tools/knowledge.py:446-473 |
| The honest report when no detection run is recorded, and the report of one run's conditions, limitations and unresolved items with no verdict either way. | `_no_detection_run`; `_condition_report` | mcp/src/agents_remember/mcp/tools/knowledge.py:475-483; mcp/src/agents_remember/mcp/tools/knowledge.py:486-523 |
| The project builder: the destination profile, the refusal when no view was named, the delegation to the projection operation, and the per-path report relayed back. | `knowledge_project_payload`; `_refused_project` | mcp/src/agents_remember/mcp/tools/knowledge.py:557-596; mcp/src/agents_remember/mcp/tools/knowledge.py:623-631 |
| The view-and-identity pairs one projection request set resolves into, where the recorded identity rather than the destination path is what the artifact is about. | `_projection_requests` | mcp/src/agents_remember/mcp/tools/knowledge.py:658-679 |
| The closed set of record kinds this surface may be asked about, the unconditional refusal every one of them earns, and the constant naming the entry point that can actually write. | `DECLARED_CHANGE_KINDS`; `WRITE_ENTRY_POINT` | mcp/src/agents_remember/mcp/tools/knowledge.py:81-88; mcp/src/agents_remember/mcp/tools/knowledge.py:92-92; mcp/src/agents_remember/mcp/tools/knowledge.py:68-92 |
| The flat read request as one frozen value, with the continuation, the two revision selectors, the optional source-path seed and the optional source roots it carries. | `ReadToolRequest`; `source_path` | mcp/src/agents_remember/mcp/tools/knowledge.py:96-114; mcp/src/agents_remember/mcp/tools/knowledge.py:112-112 |
| The validated view request with its continuation reconstructed opaquely and its source-path seed forwarded, its snapshot binding left to the seam. | `_view_request`; `ViewContinuation`; `source_path` | mcp/src/agents_remember/mcp/tools/knowledge.py:262-287; mcp/src/agents_remember/models/knowledge/view.py:370-370; mcp/src/agents_remember/models/knowledge/view.py:370-381 |
| The record builder and its unconditional `registration_absent` refusal, naming the subcommand that owns the write and reporting no missing destination for a write this surface cannot make. | `knowledge_change_payload`; "registration_absent"; `WRITE_ENTRY_POINT` | mcp/src/agents_remember/mcp/tools/knowledge.py:103-103; mcp/src/agents_remember/mcp/tools/knowledge.py:307-322 |
| The integrity builder that returns `compatible: None` present beside its explicit unresolved list, and the recorded-conditions read with its connection closed in a `finally`. | `knowledge_integrity_check_payload`; `_recorded_conditions`; "compatible": None | mcp/src/agents_remember/mcp/tools/knowledge.py:408-489 |
| The honest report when no detection run is recorded, the scope-bound run lookup that never reports another scope's conditions, the report for a scope no recorded run measured, and the report of one run's conditions, limitations and unresolved items with no verdict either way. | `_no_detection_run`; `_run_for_scope`; `_no_run_for_scope`; `_condition_report` | mcp/src/agents_remember/mcp/tools/knowledge.py:491-500; mcp/src/agents_remember/mcp/tools/knowledge.py:503-520; mcp/src/agents_remember/mcp/tools/knowledge.py:522-534; mcp/src/agents_remember/mcp/tools/knowledge.py:536-554 |
| The strict response types these builders' payloads are validated against, with `compatible` typed `None` and the view payload travelling as the view's own JSON. | `KnowledgeIntegrityCheckResponse`; `KnowledgeReadResponse`; `compatible: None = None` | mcp/src/agents_remember/models/tools/knowledge_responses.py:85-104; mcp/src/agents_remember/models/tools/knowledge_responses.py:35-52 |

## Cross-Repo References

No cross-repository behavior is implemented in this file. Each builder mediates one caller's request into one namespace's dataset or destination directory, and every vocabulary it names — the five views, the declared change kinds, the projection formats — is defined in this repository.

| Finding | Anchor | Source |
| --- | --- | --- |
| No meaningful cross-repo references found. | — | — |

## Update History
- 2026-09-20T02:05:20+00:00: Generated citation repair: `__all__` repointed to mcp/src/agents_remember/mcp/tools/knowledge.py:67-77. No content impact: mechanical anchor-range projection bound to citation source snapshot fe7fdbf3f561fa23007ac47565833928a7c74df1b4b028969d78a5b139bb65b8; claim bytes unchanged; generated by ccr-r10@v1.
- 2026-09-20T02:05:20+00:00: Generated citation repair: `DECLARED_CHANGE_KINDS` repointed to mcp/src/agents_remember/mcp/tools/knowledge.py:92-99. No content impact: mechanical anchor-range projection bound to citation source snapshot fe7fdbf3f561fa23007ac47565833928a7c74df1b4b028969d78a5b139bb65b8; claim bytes unchanged; generated by ccr-r10@v1.
- 2026-09-20T02:05:20+00:00: Generated citation repair: `_refused_read` repointed to mcp/src/agents_remember/mcp/tools/knowledge.py:162-170. No content impact: mechanical anchor-range projection bound to citation source snapshot fe7fdbf3f561fa23007ac47565833928a7c74df1b4b028969d78a5b139bb65b8; claim bytes unchanged; generated by ccr-r10@v1.
- 2026-09-20T02:05:20+00:00: Generated citation repair: `_projection_requests` repointed to mcp/src/agents_remember/mcp/tools/knowledge.py:658-679. No content impact: mechanical anchor-range projection bound to citation source snapshot fe7fdbf3f561fa23007ac47565833928a7c74df1b4b028969d78a5b139bb65b8; claim bytes unchanged; generated by ccr-r10@v1.
- 2026-09-20T02:05:20+00:00: Generated citation repair: `knowledge_change_payload`; `WRITE_ENTRY_POINT`; "registration_absent" repointed to mcp/src/agents_remember/mcp/tools/knowledge.py:307-322; mcp/src/agents_remember/mcp/tools/knowledge.py:103-103; mcp/src/agents_remember/mcp/tools/knowledge.py:311-311. No content impact: mechanical anchor-range projection bound to citation source snapshot fe7fdbf3f561fa23007ac47565833928a7c74df1b4b028969d78a5b139bb65b8; claim bytes unchanged; generated by ccr-r10@v1.
- 2026-09-20T02:24+02:00 — 260915-KS-L32 curator, post-sync citation pass (uncommitted change set on `ar/260915-ks-l32-ar`, code base `7ca3ac48914a562bb90b5fe04d6c17b5a3f51d80`, memory base `4ffb8d8d3ff372847784fe8f7d2a13e57d9509a5`): **read the one `citation_claim_reopened` row on this card line by line against the merged code tree and disposed of it as RETAINED, and dropped the stale 2026-09-19T22:28:52+00:00 history line that was the only thing still holding the claim open.** The row — "the flat read request as one frozen value, with the continuation, the two revision selectors, the optional source-path seed and the optional source roots it carries" — was re-read rather than trusted, because unchanged claim bytes are not evidence that the claim still holds at a newly projected range. (1) **Does the construct the range covers support the claim's own words? Yes, element by element.** `@dataclass(frozen=True)` at `95` and `class ReadToolRequest:` at `96` open the frozen value; its body carries `database_path` (`104`), `repository_id` (`105`) and `view` (`106`); `continuation` (`109`) is the continuation; `invariant_revision_id` (`110`) and `family_revision_id` (`111`) are the two revision selectors; `source_path` (`112`) is the optional source-path seed — this leaf's own addition, forwarded by `_view_request` at `283`; and `repository_root` (`113`) beside `code_tree_id` (`114`) are the optional source roots. Every element the Finding names is present, in the order it names them, inside the cited `96-114`. (2) **Was the range rebound from a mention to a declaration elsewhere? No.** `96-114` is the class's own declaration extent — the location the claim is *about* — and `112-112` is the seed's own binding line, so the citation points at the content the prose describes rather than at a same-named declaration somewhere else. The stale automated history line had instead spelled the extent `94-112`, the decorator plus a two-line-short body that excluded `repository_root` and `code_tree_id`; that line no longer described the range this row carries, so it is **retired**, exactly as this card retired its 2026-09-18T17:30:57+00:00 record. Every other generated history line on this card names a *different* claim's anchors and was left alone. **Disposition: wording retained** — its words are exactly what the extent holds — **no range changed in this pass, no anchor added or removed, no citation dropped, no other row touched.** **Stamp accounting:** the card keeps its `lastVerifiedCommitHash`/`lastVerifiedCommitDate` pair unchanged and its single `reviewedWorkingCandidate` row naming `ar/260915-ks-l32-ar` at base `7dcec036`; no commit hash was invented and no stamp was advanced — the candidate is uncommitted and the governed closeout owns the real code and memory commits.
- 2026-09-20T02:12+02:00 — 260915-KS-L32 memory-side conflict resolution (uncommitted; this worktree, code base `7dcec036`, merged tree = L30's landed `7ca3ac48` plus this leaf's four modified paths): **resolved the metadata block and fourteen reference rows.** The metadata keeps upstream's verification stamp — `lastVerifiedCommitHash 7ca3ac48…` with its date, the landed commit this worktree stands on — and this leaf's `reviewedWorkingCandidate` row naming `ar/260915-ks-l32-ar` at base `7dcec036`; the superseded `ar/260915-ks-l20` candidate row this leaf's own recorded pass replaced was not restored, because two candidate rows for one card cannot both stand, and no stamp was advanced or invented. All fourteen rows cite this leaf's two modified paths (`mcp/tools/knowledge.py`, `mcp/registration/knowledge.py`) plus `models/knowledge/view.py`, so this leaf's ranges, anchors and corrected Finding texts were kept: upstream's `knowledge_project_payload`/`_refused_project` cells and its `67-79` range held neither anchor of their rows, while this leaf's `557-596; 623-631`, `81-88; 92-92; 68-92`, `491-500; 503-520; 522-534; 536-554` and `408-489` were each read against the constructs they name. No claim was re-worded, no anchor, row or citation dropped, and no verification stamp advanced.
- 2026-09-20T01:39:03+02:00 — 260915-KS-L32 curator (uncommitted change set on `ar/260915-ks-l32-ar`, code base `7dcec036`, memory base `66b2ae8a`): **cleared both enforced rows this card carried, and closed the verification-stamp question the reopened claim left open.** (a) `citation_claim_reopened`, detection-report row — the claim was re-read against the working tree instead of trusted: `_no_detection_run` (`492-500`), `_run_for_scope` (`503-520`), `_no_run_for_scope` (`522-534`) and `_condition_report` (`536-554`) each hold the construct the row names; `_run_for_scope` really does match the caller's scope against the run's own `governing_route_id` (`517`) and keep searching past a run measured over another one, and `_no_run_for_scope` really does state the limitation and the unresolved item rather than another run's conditions. Wording and ranges retained because they are the ones that hold; the two constructs the reopened row names are this leaf's own new helpers and exist only in the candidate. (b) The card's stale `lastVerifiedCommitHash`/`lastVerifiedCommitDate` pair — and the superseded `ar/260915-ks-l20` candidate row that sat beside them — are replaced by the single `reviewedWorkingCandidate` row above: this pass measured the working tree at base `7dcec036`, no commit carries these bytes, and the governed closeout owns the real stamp; no hash was invented and none was advanced. The 2026-09-18T17:30:57+00:00 record was retired for that same reason: it described an earlier automated projection of these ranges, and the ranges are now read directly against the candidate. (c) `citation_source_duplicate`, label-filter row — the row carried the same own-file source twice because two older pointers to `_supplied_effect_labels` were both repointed to its one current extent in the previous pass; the repetition is collapsed to one copy, which loses no evidence because both cells named the same construct in the same file. Every other cell of that row is left as written and was verified: `16-16` holds "inferred from the diff", and `391-405` holds `_supplied_effect_labels` at `391` and `supplied_by` at `403`. No claim was re-worded to fit a stale pointer, no anchor or range was dropped to silence a row, and no verification stamp was advanced beyond what was measured.
- 2026-09-20T01:29+02:00 — 260915-KS-L30 curator (uncommitted change set on `ar/260915-ks-l30-ar`, base `7dcec036094768c5f50e571fb45e59a27ae78efc`): **cleared the last three citation rows of this card — one dead anchor and two stale ranges.** (a) The admitted-kinds row named `ADMITTED_CHANGE_KINDS`, which exists nowhere in the tree: the construct was renamed `DECLARED_CHANGE_KINDS` and its semantics moved with the name — the module now says the surface does NOT write, the set is "what the surface may be *asked* for", and the refusal is unconditional for every member with the reason naming the real write entry point. The Anchor cell names the surviving constant and the Source cell cites its own two spans: the comment block that states why the surface does not write (`67-79`) and the declaration itself (`80-87`), replacing the three ranges (`55-59`, `63-63`, `66-67`) that the module's growth had left pointing into the unrelated `__all__` statement. The Finding text is unchanged. (b) The label-filter row cited `_supplied_effect_labels` at `267-281` and again at `300-314`; both were stale, the declaration is now `388-402` and its call site sits in the comparison builder, so the two ranges were repointed to `388-402` (the filter's own declaration span) and `318-333` (the builder head in which the filter is applied at 322) — no range was dropped, no duplicate source was introduced, and the row's other four citations are untouched. (c) The integrity row cited `284-333` for three anchors; it was repointed to the two constructs' own spans, `405-443` for `knowledge_integrity_check_payload` (which carries `"compatible": None` at 431 and 440 beside its explicit unresolved list) and `446-472` for `_recorded_conditions` (whose `finally: connection.close()` is at 470-471). No claim wording, anchor or citation was added, removed or re-worded anywhere in this document, and no verification stamp was advanced. **Two further ranges in this document were repointed as pure moves**, the checklist having reported them report-only ("still resolves ONCE ... STALE BY A MOVE"): `_view_request` `175-195` → `260-284` and `knowledge_change_payload` `291-291` → `287-315`. Those are the checklist's own resolved extents for those anchors on this candidate, no claim content changes with them, and the same sweep had already recorded `_recorded_conditions` `446-472` and `knowledge_integrity_check_payload` `405-443`, both of which (c) above adopts.
- 2026-09-20T01:20+02:00 — 260918-TSIP-L11 closing seat (memory worktree `84152b9e`, code `79fa817f`): re-read and re-derived 4 claim row(s) on the merged tip. Every row was read against the construct it cites before its range was regenerated: the merged `mcp/tests/test-evidence-lanes.toml` was read at the line that carries each lane anchor, `pyproject.toml` was read at its declaration, and every renamed or consolidated case was re-anchored on the successor whose own docstring records the consolidation. No range was produced by adding a delta to an old number and the product's mechanical fixer was not run, so **no projection bullet is written and no claim is reopened on this edit's account**. Rows: `knowledge.py.md:78` (`_supplied_effect_labels`) — re-read the claim against the landed source: the construct moved and the cited range was widened to the line that actually carries it, per the checker's own remedy; `knowledge.py.md:79` (`_recorded_conditions`) — re-read the claim against the landed source: the construct moved and the cited range was widened to the line that actually carries it, per the checker's own remedy; `knowledge.py.md:80` (`_no_detection_run`, `_condition_report`) — re-read the claim against the landed source: the construct moved and the cited range was widened to the line that actually carries it, per the checker's own remedy; `knowledge.py.md:81` (`knowledge_project_payload`) — re-read the claim against the landed source: the construct moved and the cited range was widened to the line that actually carries it, per the checker's own remedy.
- 2026-09-20T00:46:52+02:00 — 260915-KS-L32 curator (uncommitted change set on `ar/260915-ks-l32-ar`, code base `7dcec036`, memory base `66b2ae8a`): **body update for this leaf's scope-binding repair, and two corrections the report's own findings forced.** (a) The integrity check's lookup is now bound to the requested scope: `_recorded_conditions` hands its rows to `_run_for_scope`, which matches the caller's scope against the run's own `governing_route_id` and keeps searching past a run measured over another scope, and `_no_run_for_scope` reports a scope no recorded run measured instead of handing back a different run's conditions under the requested scope's name. The integrity paragraph, a new invariant, and the report row (now also carrying `_run_for_scope` and `_no_run_for_scope`) state that. (b) The read request gained `source_path`, forwarded by `_view_request`, so the read paragraph and the request row now say so. (c) **Corrected two claims that were already false at the base commit** and were not caused by this leaf: the card asserted `ADMITTED_CHANGE_KINDS` was `("evidence_claim", "verification_observation")` with a second `registration_absent` branch for missing destination facts, but no such constant exists anywhere in the code tree — the module declares `DECLARED_CHANGE_KINDS` (six kinds: `evidence_claim`, `verification_observation`, `invariant_revision`, `assumption`, `semantic_change_set`, `requirement_revision`) precisely because "the earlier spelling advertised two 'admitted' kinds and then refused both anyway", and `knowledge_change_payload` has exactly one unconditional refusal branch naming `WRITE_ENTRY_POINT`. The commentary, the Conventions sentence, the invariant and the row now state the code. Citation repair in the same pass: the read-request row `62-80` → `96-114` (+`112-112`), the validated-view-request row `175-195` → `262-287`, the record-builder row `198-229` → `290-318`, the label-filter row's own-file cell `300-314` → `391-405`, the integrity row `284-333` → `408-489` (so the ranges actually hold `"compatible"`, carried at `416`, `434` and `443`), the detection-report row `373-381; 384-402` → `492-500; 503-520; 522-534; 536-554`, the project-builder row `368-407; 471-479` → `557-596; 623-631`, and the projection-requests row `410-431` → `599-621`. No claim was re-worded to fit a stale pointer, no anchor or range was dropped to silence a row, and no verification stamp advanced: the candidate is uncommitted and the governed closeout owns the real code and memory commits.
- 2026-09-20T00:31+02:00 — 260915-KS-L30 curator (uncommitted change set on `ar/260915-ks-l30-ar`, base `7dcec036094768c5f50e571fb45e59a27ae78efc`): **mechanical citation-range projection** against this leaf's candidate. The checklist reported 5 row(s) whose cited range no longer holds its anchor although the construct is present in the cited file; each range was widened to the lines that carry it — `_condition_report`; `_no_detection_run`; `_refused_project`; `knowledge_project_payload`; `registration_absent`. No claim wording, anchor or citation was added, removed or re-worded, and no range was deleted: the new range is the checklist's own resolved extent for that anchor on this candidate. No verification stamp was advanced.
- 2026-09-19T22:28:52+00:00: Generated citation repair: `__all__` repointed to mcp/src/agents_remember/mcp/tools/knowledge.py:55-65. No content impact: mechanical anchor-range projection bound to citation source snapshot 440311ed835ff15c77271ad85c2bef2103d2b46ebe061b96476b211b3d19cd24; claim bytes unchanged; generated by ccr-r10@v1.
- 2026-09-19T22:28:52+00:00: Generated citation repair: `ChangeToolRequest`; `DiffToolRequest`; `ProjectToolRequest` repointed to mcp/src/agents_remember/mcp/tools/knowledge.py:115-122; mcp/src/agents_remember/mcp/tools/knowledge.py:125-133; mcp/src/agents_remember/mcp/tools/knowledge.py:136-146. No content impact: mechanical anchor-range projection bound to citation source snapshot 440311ed835ff15c77271ad85c2bef2103d2b46ebe061b96476b211b3d19cd24; claim bytes unchanged; generated by ccr-r10@v1.
- 2026-09-19T22:28:52+00:00: Generated citation repair: `_refused_read` repointed to mcp/src/agents_remember/mcp/tools/knowledge.py:149-157. No content impact: mechanical anchor-range projection bound to citation source snapshot 440311ed835ff15c77271ad85c2bef2103d2b46ebe061b96476b211b3d19cd24; claim bytes unchanged; generated by ccr-r10@v1.
- 2026-09-19T22:28:52+00:00: Generated citation repair: `knowledge_read_payload`; "unknown_view"; "not one of the five named query views" repointed to mcp/src/agents_remember/mcp/tools/knowledge.py:192-257; mcp/src/agents_remember/mcp/tools/knowledge.py:211-211; mcp/src/agents_remember/mcp/tools/knowledge.py:212-212. No content impact: mechanical anchor-range projection bound to citation source snapshot 440311ed835ff15c77271ad85c2bef2103d2b46ebe061b96476b211b3d19cd24; claim bytes unchanged; generated by ccr-r10@v1.
- 2026-09-19T22:28:52+00:00: Generated citation repair: `knowledge_diff_payload`; `_diff_request` repointed to mcp/src/agents_remember/mcp/tools/knowledge.py:318-369; mcp/src/agents_remember/mcp/tools/knowledge.py:372-385. No content impact: mechanical anchor-range projection bound to citation source snapshot 440311ed835ff15c77271ad85c2bef2103d2b46ebe061b96476b211b3d19cd24; claim bytes unchanged; generated by ccr-r10@v1.
- 2026-09-19T22:28:52+00:00: Generated citation repair: `_projection_requests` repointed to mcp/src/agents_remember/mcp/tools/knowledge.py:549-570. No content impact: mechanical anchor-range projection bound to citation source snapshot 440311ed835ff15c77271ad85c2bef2103d2b46ebe061b96476b211b3d19cd24; claim bytes unchanged; generated by ccr-r10@v1.
- 2026-09-19T22:38+02:00 — 260918-TSIP-L11 curator (memory worktree `fd1a024e`, code `7879f5b2`): **re-read each claim below against the construct its range now covers, corrected the wording where the construct had moved, re-derived every range from the construct's real extent in the file the claim cites, and only then left the card's stamp to closeout.** No `Generated citation repair` bullet is written: these are curator edits, not a mechanical projection. `knowledge.py.md:70` — re-read: the constant was renamed to DECLARED_CHANGE_KINDS and the claim as written is now FALSE -- the surface has no admitted write operation for ANY kind, and the refusal is unconditional for every member. Wording corrected, then the range re-derived from the file. `knowledge.py.md:76` — re-read: the payload now answers ONE unconditional registration_absent refusal whose reason is deliberately the same for a declared kind and an unheard-of one; the claim's 'both ... one naming ... one naming' split is no longer true. Wording corrected, range re-derived to the builder's real extent. `knowledge.py.md:79` — re-read: both constructs exist with the described behaviour (the integrity builder returns compatible None beside its unresolved list; the recorded-conditions read closes its connection in a finally). Only the range was stale; retained wording, re-derived extents.
- 2026-09-19T22:33+02:00 — 260918-TSIP-L11 curator (memory worktree `fd1a024e`, code `7879f5b2`): cleared the inherited citation debt on 3 claim(s) by RE-READING each claim against the merged tree and RE-DERIVING every cited range from the construct's real extent in the file the claim cites (`extents.anchor_extents`), never by adding a delta to an old number and never through the mechanical projection (no generated citation-repair bullet is written, so no claim is reopened by this edit). Claims re-read: `knowledge.py.md:78` (`_supplied_effect_labels`, "supplied_by", "inferred from the diff"); `knowledge.py.md:80` (`_no_detection_run`, `_condition_report`); `knowledge.py.md:81` (`knowledge_project_payload`, `_refused_project`).
- 2026-09-18T19:56:02+02:00 — 260915-KS-L23 residue clearance, seat B (uncommitted change set on `ar/260915-ks-l23`, memory base `59eab7a0`): **cleared the three enforced `citation_anchor_absent_from_range` rows in this document** (three table rows). (a) The admitted-kinds row cited `66-66`, the last comment line above the tuple; it was widened to `66-67` so the `ADMITTED_CHANGE_KINDS` declaration at `67` is inside it. (b) The project-builder row cited `434-442` for `_refused_project`; this leaf's changes left the helper's definition at `471-479`, which is what that cell cites now. (c) The label-filter row cited `283-283` for `_supplied_effect_labels`, whose definition now spans `300-314`; that cell cites it. Claims, anchors and every other range are unchanged. No claim was re-worded, no anchor or range was dropped to silence a row, and no verification stamp advanced: the candidate is uncommitted and the governed closeout owns the real code and memory commits.
- 2026-09-18T15:30+02:00 — 260915-KS-L20 curator (uncommitted change set on `ar/260915-ks-l20`, base `9f88a6de`): created this one-to-one card for the five mounted `knowledge_*` payload builders. It records that nothing here decides anything — no classification computed, no effect label inferred, no draft authored, no rationale judged, no ambiguity resolved by choosing, no missing assessment filled and no compatibility verdict produced — and that each builder returns the unresolved state wherever a decision would otherwise be required: the `registration_absent` refusal for a kind with no admitted write operation, the view payload returned as itself so the classification rule has one implementation, the diff carrying only labels an identified agent or assessment supplied, and `compatible: None` sitting beside an explicit unresolved list. The deliberate absences are stated too: no renderer, no detector, no diff algorithm and no write path of its own. This card carries **no `lastVerifiedCommitHash`**: every construct it cites exists only in this leaf's uncommitted candidate, so no real commit contains the content a stamp would claim to have verified. The `reviewedWorkingCandidate` row states what was actually read, and closeout owns the stamp once the code commit exists.
