# mcp/src/agents_remember/worktrees/integration/closeout/preparation/code_output.py

| Field | Value |
| --- | --- |
| repository | agents-remember |
| path | `mcp/src/agents_remember/worktrees/integration/closeout/preparation/code_output.py` |
| doc_type | `file-level-onboarding` |
| lastUpdated | 2026-09-06T17:13:06+00:00 |
| lastVerifiedCommitHash | |
| lastVerifiedCommitDate | |
| governingOverview | `overview.md` |

## Governing Overview

[Owning overview](overview.md)

## Purpose

Selected code preparation intent and original prefix binding.

## Code Commentary

### Logic

Code preparation binds the exact original four-certificate prefix, frozen run and candidate authorities before private execution. Current configuration, hook policy, logical Git preimages and enabled/no-write inputs remain part of the immutable intent. Output retention delegates to the shared raw-output selector.

### Conventions

Use the named source owners directly. This source was introduced in landed commit `245057ab16e19afdaabd5c188c9576b22e0c0870` and remains byte-identical at the recovery code candidate. Its behavior was re-read against that source during memory recovery; the existing metadata owner still owns the pending verification stamp.

### Invariants And Boundaries

The documented types and paths do not themselves establish execution, certification, delivery or acceptance. Those claims require the corresponding owning runtime evidence.

### Todos

No source-local TODO is asserted here.

## Docs References

| Finding | Anchor | Source |
| --- | --- | --- |
| No configured domain documentation applies. | N/A | N/A |

## Repo-Internal References

| Finding | Anchor | Source |
| --- | --- | --- |
| `current_code_preparation` owns the corresponding behavior described above. | `current_code_preparation` | `mcp/src/agents_remember/worktrees/integration/closeout/preparation/code_output.py:35-51` |
| `select_code_preparation` owns the corresponding behavior described above. | `select_code_preparation` | `mcp/src/agents_remember/worktrees/integration/closeout/preparation/code_output.py:54-70` |
| `_code_prefix` owns the corresponding behavior described above. | `_code_prefix` | `mcp/src/agents_remember/worktrees/integration/closeout/preparation/code_output.py:73-84` |
| `_new_code_intent` owns the corresponding behavior described above. | `_new_code_intent` | `mcp/src/agents_remember/worktrees/integration/closeout/preparation/code_output.py:87-133` |
| `retain_code_output` owns the corresponding behavior described above. | `retain_code_output` | `mcp/src/agents_remember/worktrees/integration/closeout/preparation/code_output.py:136-140` |

## Cross-Repo References

| Finding | Anchor | Source |
| --- | --- | --- |
| No cross-repository source is needed for this card. | N/A | N/A |

## Update History

### 2026-09-06T17:13:06+00:00 — Initial L34 implementation card

Created from the current source. Verification metadata is intentionally unset until a genuine commit-based verification occurs; no test or acceptance result is asserted.
