# PDLS Onboarding Coverage Plan

## Required Coverage

| Route | Required outcome |
| --- | --- |
| repository guidance | Raw host pytest remains prohibited; the exact-node wrapper is supported and non-certifying. |
| `mcp/src/agents_remember/testing` | One route overview and one card per source file explain classifier, bootstrap, admission, runner, and phase ownership. |
| `mcp/src/agents_remember/models` | Diagnostic/certifying evidence altitude and consumer firewall are explicit. |
| `mcp/src/agents_remember/code_quality` | The wrapper and retry proof consume typed admission; the deleted validator has no compatibility card. |
| `mcp/src/agents_remember/worktrees` | Immutable schema-2 Dagger publication is the certifying evidence factory for lifecycle consumers. |
| `mcp/tests` | New forcing/cohort cards exist; moved helpers and relocated assertions preserve their history. |
| `scripts/test-python` | The one direct command is documented with its fixed execution boundary. |

## Validation

1. Every added/moved in-scope source has one exact sidecar.
2. Every changed existing source card has a content update or explicit `No content impact:` history
   attestation and current source commit metadata.
3. Deleted/moved predecessor cards are absent and their useful knowledge is preserved at successors.
4. Governing overviews and `system/tools.md` agree on one command/evidence truth.
5. Route indexes are regenerated only after the content tree is final.
