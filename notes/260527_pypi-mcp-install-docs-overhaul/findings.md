# Findings: PyPI MCP Install And User Documentation Overhaul

**Task:** `260527_pypi-mcp-install-docs-overhaul`
**Created:** 2026-05-27T13:02

---

## Purpose

Record every defect, friction point, mismatch, and workflow-integrity issue found while validating the PyPI MCP install path, Codex harness setup, provider setup, chat mode, light tasks, worktrees, memory adoption, and closeout.

The documentation rewrite should only describe happy paths that pass after fixes and retests. Deferred or unresolved issues stay visible here and should not be papered over in user docs.

---

## Status Values

- `open`
- `fixing`
- `fixed-awaiting-retest`
- `retested`
- `deferred`
- `docs-only`
- `mcp-retest-required`

---

## Findings

| ID | Area | Severity | Status | Summary | Repro / Evidence | Expected | Actual | Fix / Owner | Retest |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| F-001 | Memory init | high | open | MCP `memory_init` does not create `system/settings.json` for a new memory root. | Pass 02 `memory_init(repo_id="agents-remember-md", initialize_git=true)` via child Codex/PyPI MCP created only `system/settings.md`, `system/tools.md`, and `system/sources.md`; `context_packet` then reported `pathSettingsPath: ""` and empty `pathRules`. | New memory scaffold includes machine-readable storage/path-rule settings so resolver/drift/docs have a concrete authority file. | MCP path confirms no machine-readable memory settings file is created. | Implementation defect candidate in memory init scaffold; docs must not claim `system/settings.json` exists after first init until fixed. | Reproduced through MCP in pass 02. |
| F-002 | Repo-local memory | high | open | MCP context tools disagree on repo-local `ar-memory/`: resolver finds it, context/baseline route to missing external memory. | Pass 02 repo-local fixture with `ar-memory/system/*` and `ar-memory/onboarding/README.md.md`: `resolve_context(repo-local-memory-repo)` returned memory mode `internal` at repo-local `ar-memory`; `context_packet` returned memory mode `external` at missing `/ar-coordination/memory-repos/ar-repo-local-memory-repo`; `memory_baseline_status` failed. | MCP-facing context, drift, and baseline tools should agree with resolver topology and use repo-local `ar-memory/` when it exists. | Repo-local `ar-memory/` is still unusable through `context_packet` and baseline tools. | Implementation defect candidate in MCP context/baseline resolver parity. | Reproduced through MCP in pass 02 after adding onboarding to the fixture. |
| F-003 | Worktree cleanup | high | mcp-retest-required | Worktree code/memory cleanup passes through MCP, but provider-runtime cleanup remains unvalidated because provider lifecycle is not isolated. | Pass 02 `worktree_cleanup(dry_run=false)` removed code and memory worktrees and deleted merged task branches when provider setup was skipped. The earlier direct-API fake provider-runtime cleanup repro is not cleared because no worktree provider runtime was safely created through MCP. | Cleanup removes code worktree, memory worktree, merged task branches, and worktree-specific provider runtime state. | Code/memory cleanup passes; provider-runtime cleanup remains blocked by provider isolation risk F-010. | Retest after provider lifecycle can be safely isolated or namespaced. | Partially retested through MCP in pass 02; provider-runtime portion not retested. |
| F-004 | Provider startup validation | high | mcp-retest-required | True provider up/down via MCP was blocked because the isolated PyPI run saw shared/global provider containers. | Pass 02 `provider_status` via child Codex/PyPI MCP succeeded and reported durable daemon namespace, but GrepAI status used generic containers and listed unrelated projects. Real stop/start was not run to avoid mutating shared host provider state. | Watcher up/down invoked as MCP tools should target only the configured isolated workspace/provider roots. | Source-level provider isolation now passes Docker integration, but PyPI-installed MCP up/down still needs retest after packaging/installing these changes. | Source implementation added namespaced provider instances and workflow-local provider setup; rerun PyPI MCP provider status/up/down next. | MCP retest required after source changes are installed. |
| F-005 | Harness visibility | medium | docs-only | Fresh Codex sessions need the MCP server and installed skills loaded separately. | Pass 02 child Codex with PyPI MCP config successfully called `server_info` and returned config path `/tmp/ar-mcp-real-validate-02/.codex/mcp/settings.json`; a later run with `CODEX_HOME=/tmp/ar-mcp-real-validate-02/.codex` proved the PyPI-installed skills were visible from temp Codex home. | Manual gives first-time users concrete restart/reload validation: ask the model to call `server_info`, and verify installed skills are in the harness skill root after skill install/restart. | MCP server visibility and skill visibility are separate checks; installing skills alone does not affect already-running sessions. | Documentation: harness registration, restart/reload, and validation wording. | Retested through fresh child Codex sessions in pass 02. |
| F-006 | Memory init Git branch | low | docs-only | MCP `memory_init` runs plain `git init`, creating a transient `master` branch and Git warning before baseline adoption moves to `main`. | Pass 02 `memory_init(... initialize_git=true)` MCP result included Git stderr warning “Using 'master' as the name for the initial branch”; host inspection showed `No commits yet on master`. `memory_baseline_adopt` then created ledger commits on `main`. | First-run memory scaffolding should avoid branch-name surprise, or docs should clearly say baseline adoption creates the first ledgered `main` state. | Transient `master` appears before adoption, but baseline adoption succeeds and ends on `main`. | Documentation note or implementation improvement to use `git init -b main`. | Reproduced through MCP in pass 02. |
| F-007 | Skills reinstall | low | docs-only | Re-running `skills_install` without `overwrite` or `archive_existing` errors when the target skill tree already exists. | PyPI `skills_install_payload(layout="tree", dry_run=true, overwrite=false)` raised `FileExistsError` after an earlier install; `overwrite=true` dry-runs for tree and flat layouts passed. | Refresh docs should tell users/model to use overwrite or archive when reinstalling skills. | First install is fine; refresh path needs explicit flags. | Documentation: skills install/refresh troubleshooting. | Not retested. |
| F-008 | Validation method | high | retested | First validation pass used direct Python/API calls; pass 02 redid workflow checks through child Codex MCP tool calls against the PyPI-installed server. | Pass 02 transcripts contain `mcp_tool_call` events for `server_info`, `runtime_install`, `skills_install`, `memory_init`, `context_packet`, provider tools, baseline tools, direct closeout, light-task closeout, worktree lifecycle, and memory-topology checks. | Workflow integrity is proven only by model/harness MCP tool calls against the installed PyPI MCP server. | Pass 02 satisfies the corrected method for the covered paths; provider mutation remains blocked by F-004/F-010. | Keep validation method contract in task and docs. | Retested through child Codex sessions using PyPI MCP executable and isolated settings. |
| F-009 | Runtime install order | high | open | `runtime_install` fails before memory initialization when GrepAI is configured because the memory root does not exist yet. | Pass 02 `runtime_install(dry_run=true)` and `runtime_install(dry_run=false)` both failed before `memory_init`: `grepai root path does not exist or is not a directory`. After `memory_init`, both runtime install calls succeeded. | First-run MCP happy path should either install runtime before memory exists or guide the model through memory init before provider-dependent runtime steps. | A first-time user asking “install runtime” first can hit a provider-root failure. | Decide whether to change runtime install ordering/behavior or document memory-init-before-provider-deps clearly. | Reproduced through MCP in pass 02. |
| F-010 | Provider isolation | high | mcp-retest-required | Isolated PyPI provider status reused generic GrepAI Docker state containing unrelated workspace projects. | Pass 02 `provider_status` and `provider_watchers(action="status", dry_run=false)` from isolated settings showed GrepAI workspace `agents-remember-memory` with 8 projects including `device-management`, `tensorflow`, and others. | Isolated validation/worktree provider setup should not watch or report unrelated global projects. | Source-level Docker integration now shows distinct source/worktree/benchmark provider instances, but the installed PyPI MCP path still needs retest. | Source implementation added provider instance ids, namespaced containers/networks/data roots, and workflow-local warm-start settings. | MCP retest required after source changes are installed. |
| F-011 | Provider watcher status | medium | open | `provider_watchers(action="status")` defaults to dry-run status and reports simulated stopped state, conflicting with `provider_status`. | Pass 02 `provider_status` reported both providers running. `provider_watchers(action="status")` returned `dryRun: true`, `ok: false`, and simulated stopped/missing containers. Explicit `provider_watchers(action="status", dry_run=false)` returned real running status. | A status call should default to real non-mutating status, or docs/tool schema should make dry-run status behavior obvious. | Default status is surprising and can make a healthy provider look stopped. | Tool default/schema/docs fix candidate. | Reproduced through MCP in pass 02. |
| F-012 | Direct closeout metadata gate | medium | open | `direct_closeout_preview` can pass even when new onboarding sidecar metadata is missing, but apply then fails. | Chat closeout retry: preview succeeded with `README.md` and `onboarding/README.md.md`; first apply failed because sidecar lacked `lastVerifiedCommitHash` and `lastVerifiedCommitDate`; after adding metadata, preview/apply succeeded. | Preview should surface all apply-blocking onboarding metadata issues before the commit-approval gate. | Apply catches the metadata issue later, after preview looked good. | Align preview checks with apply gate. | Reproduced through MCP in pass 02. |
| F-013 | Closeout onboarding friction | medium | docs-only | Direct closeout cannot finish README changes in a fresh external-memory fixture until sidecar onboarding exists and is current. | Pass 02 chat closeout preview succeeded for a README-only change, but apply blocked with `missing sidecar onboarding for: README.md`; creating/updating onboarding allowed direct closeout to complete. | Docs should tell users/model that source changes require current onboarding before closeout, especially in new repos. | The happy path is not “edit and closeout”; it is “edit, update/create onboarding, preview, apply.” | Documentation and model-facing skill wording. | Reproduced through MCP in pass 02. |
| F-014 | Closeout metadata formatting | low | open | Closeout refresh can write `lastVerifiedCommitDate` table rows without a space before the trailing pipe. | Pass 02 light-task final summary reported `direct_closeout_apply` refreshed `lastVerifiedCommitDate` as `2026-05-27T14:17:41+02:00|`. | Metadata tables should keep consistent Markdown pipe spacing. | Functional but visually sloppy metadata row formatting. | Formatting fix in closeout metadata writer. | Reproduced through MCP in pass 02. |
| F-015 | Worktree dry-run wording | low | open | Worktree dry-run outputs contain success/completed wording that can read like real mutation happened. | Pass 02 `worktree_start(dry_run=true)` returned `state: "started"`; `worktree_cleanup(dry_run=true)` returned summary “Cleanup completed” while state was `would-cleanup` and `would_remove` flags were correct. | Dry-run summaries should consistently say planned/would-run states. | Structured fields are mostly correct, but human-facing summary/state wording is misleading. | Tighten dry-run state and summary strings. | Reproduced through MCP in pass 02. |
| F-016 | GrepAI warm-start | high | retested | GrepAI provider data is copyable in principle, but Agents Remember initially had no safe project-scoped GrepAI seed/copy operation. | Provider workflow audit 01 found GrepAI mirrored roots plus PostgreSQL/pgvector tables (`documents.path`, `chunks.file_path`) but no GrepAI CLI export/load equivalent to CGC. | Worktree and benchmark provider setup can copy existing GrepAI data and let the watcher reconcile small branch/commit deltas. | Before the provider compatibility fix, new GrepAI worktree/benchmark provider instances required full re-indexing or fallback refresh. | Implemented Agents Remember-owned GrepAI DB clone plus active-project root swap/reconcile path for worktrees and benchmarks. | Retested by Docker integration: worktree and benchmark GrepAI warm-start passed. |
| F-017 | CGC worktree runtime | high | retested | CGC doctor rejected worktree-local provider runtimes because it only allowed `ar-coordination/providers/*`. | Docker integration pass 01: `worktree_start_tool` created isolated worktree providers, GrepAI was healthy, but provider setup blocked because CGC doctor reported `runtime-root-contained: false` for `worktrees/.../provider-runtime/providers/runners/codegraphcontext/...`. | Workflow-local provider runtimes under the coordination tree and outside the code checkout are accepted. | Worktree-local CGC setup was blocked despite safe runtime placement. | Fixed CGC containment check to allow coordination-owned workflow runtimes outside the source repo and added unit coverage. | Retested by Docker integration: source, worktree, and benchmark providers all passed. |
| F-018 | GrepAI DB clone | high | retested | GrepAI warm-start used a `pg_dump --single-transaction` option unsupported by the provider image. | Docker integration diagnostic rerun: worktree provider setup reported `grepai clone-db` failure with `pg_dump: unrecognized option '--single-transaction'`. | GrepAI source DB clone succeeds against the packaged pgvector/Postgres image. | Clone failed and provider setup fell back to watcher refresh, defeating the warm-start goal. | Removed the unsupported flag and added dry-run command coverage. | Retested by Docker integration: worktree and benchmark GrepAI warm-start passed. |
| F-019 | CGC seed bundle mount | high | retested | CGC export wrote seed bundles to a host temp directory that was not mounted inside the runner container. | Docker integration diagnostic rerun: CGC seed export failed with `FileNotFoundError` for `ar-coordination/temp/provider-seeds/repo-a.source.cgc` inside the container. | CGC source export and target load use bundle paths visible to the respective runner containers. | Bundle path existed only on the host, not in the container mount set. | Moved default seed bundles under source/target CGC runtime roots and added dry-run path coverage. | Retested by Docker integration: worktree and benchmark CGC seed passed. |
| F-020 | Benchmark provider warm-start | high | retested | Benchmark provider setup skipped GrepAI clone and CGC load used a prompt/shortcut path that failed in containerized runs. | Docker benchmark integration diagnostic: `grepai clone-db` skipped with `no isolated GrepAI target settings configured`; CGC `load --clear` failed through CGC's shortcut/prompt behavior. | Benchmark-local providers warm-start from the source provider using generated benchmark settings and non-interactive CGC import. | Benchmark setup reached healthy final watchers but still returned aggregate failure from earlier seed/clone phases. | Let GrepAI clone use non-isolated target settings and changed CGC seed load to non-interactive `bundle import` for fresh workflow-local targets. | Retested by Docker integration: benchmark provider prepare/status passed for both providers and cleanup left no test containers. |
| F-021 | CGC refresh parallelism | high | fixed-awaiting-retest | CGC `refresh-all` reindexed repositories sequentially and created watcher containers only after the whole refresh phase finished. | Main provider reinstall against the workspace settings started `refresh-all`; Docker showed only the namespaced FalkorDB backend and one transient runner indexing `Cobalt`, while no namespaced per-repo watcher containers existed yet. | A re-refresh/reinstall should create all configured CGC watcher containers in one bulk compose operation and start every repo index job in parallel; one repo should never block the next repo from starting. | `cgc_start_all` and `cgc_refresh_all` delegated through a layout loop, so start and refresh operations were serialized by repo. | Changed `cgc_start_all` to use one bulk `docker compose up -d` across watcher services and changed `cgc_refresh_all` to start watchers first, then fan out repo index jobs concurrently. | Unit tests passed for bulk start command and concurrent two-repo refresh; main-provider reinstall retest pending. |
| F-022 | CGC runner dependencies | high | fixed-awaiting-retest | Disposable CGC runner commands repeatedly replaced the namespaced FalkorDB backend container. | Main provider reinstall events showed `runner doctor` one-off containers exiting with code 0, followed by Compose killing/destroying/recreating `ar-cgc-falkordb-workspace-61ebc7e695` several times. | Runner commands that already have an explicitly started backend should not manage or recreate backend dependencies. | `docker compose run --rm runner ...` let Compose reconcile dependencies and replace FalkorDB on each runner call. | Changed CGC doctor, refresh, and bounded query runner calls to use `docker compose run --rm --no-deps ...`. | Provider lifecycle/setup unit tests passed; main-provider reinstall retest pending. |
| F-023 | CGC runner render selection | high | fixed-awaiting-retest | Settings-backed one-repo CGC runner commands rendered only the selected repo service, and parallel refresh shared one runner mount across all repos. | Targeted `cgc run --repo-id agents-remember-md -- --help` after all watchers were up emitted Docker Compose orphan warnings for the other repo watcher containers; code review showed `refresh-all` was passing one render whose runner volume came from the first layout. | Bounded runner commands should mount the target repo as the runner repo while keeping all configured watcher services in the Compose render to avoid orphan warnings. | Selected-repo renders hid sibling watcher services, and shared refresh renders could mount the wrong repo for a parallel index worker. | Added a project-layout render helper that orders the target repo first while retaining all watcher services; refresh/query runner calls now use it. | Unit test verifies each parallel refresh worker renders its own repo mount and includes both watcher services; main-provider refresh retest pending. |
| F-024 | Provider naming | medium | fixed-awaiting-retest | Provider instance ids were opaque hash-first values in Docker-facing names, making it hard to tell which workspace/workflow owned a container. | Docker Desktop showed names such as `agents-remember-cgc-workspace-61ebc7e695` and `ar-cgc-falkordb-workspace-61ebc7e695`, which do not communicate the workspace or workflow to a user. | Normal providers use the workspace folder name, worktree providers combine workspace folder plus worktree name, benchmark providers combine workspace folder plus `benchmark`, and duplicate workspace names can use readable numbered explicit ids such as `projects_2`. | The hash-first id was technically unique but poor operational UX and especially confusing while debugging provider cleanup. | Changed default provider identity derivation to readable workspace/workflow names, fixed benchmark registration to write the benchmark instance id explicitly from the benchmark workspace, and kept explicit `instanceId` for numbered duplicate workspaces. | Focused config/provider setup/benchmark registration tests passed; main-provider reinstall retest pending. |
| F-025 | Provider setup reporting | medium | fixed-awaiting-retest | Main provider `prepare` can return top-level `ok=false` while leaving both providers healthy and serving useful data. | Main workspace provider reinstall using `main-provider-settings.json` exited with top-level `"ok": false`. Follow-up probes on 2026-05-28 showed `watchers status` `ok=true`, GrepAI status `ok=true`, CGC backend status `ok=true`, all eight CGC watchers running, CGC `stats` working for every configured repo, CGC symbol lookups working for `agents-remember-md`, `device-management`, and `tensorflow`, and GrepAI repo-scoped searches returning indexed data. The full failing sub-result from the original huge JSON payload was not retained. | `prepare` should either return `ok=true` after successful final health/status, or preserve and summarize the exact failed phase so users can distinguish broken data from a recoverable/false-negative setup phase. | Source fix now preserves compact failed phases, final watcher status, result counts, setup summary paths, and a separate `ready/state` view while keeping strict top-level `ok`; main provider prepare needs retest to prove the next failure is diagnosable. | Added provider setup reporting fields and compact summaries under `logs/providers/setup/last-<action>.json` plus timestamped snapshots. | Retest main provider prepare after reinstall/published MCP update and verify `failedPhases`, `finalStatus`, and setup summary file capture any remaining false. |
| F-026 | CGC provider runtime | high | retested | CGC still exposed host-venv compatibility surface after managed execution moved to Docker. | Code and onboarding review found `CgcRuntimeLayout.venv_root`, `cgc_executable()`, `venvRoot` parsing/emission in settings fixtures, host site-packages patch discovery helpers, and lifecycle patch/status helpers that searched coordination-root venv paths. | Managed CGC must not install, discover, patch, or fall back to executables inside the user-owned coordination root. Stale `venvRoot` settings should fail visibly. | The old surface was mostly unused, but keeping it around contradicted the Docker-only provider contract and made the manual/onboarding ambiguous. | Removed the layout field, executable accessor, settings emission, host-venv discovery helpers, and lifecycle host-venv patch/status helpers. Added stale `venvRoot` rejection coverage and tightened onboarding. | Retested with focused provider tests and the full source quality wrapper on 2026-05-28. |

---

## Retest Summary

Pass 02 reran the workflow-integrity validation through child Codex sessions using the PyPI-installed MCP executable and isolated MCP settings. Chat closeout, light-task closeout, worktree start/closeout/integrate/cleanup, brand-new memory init/adoption, and existing external-memory adoption were exercised through real MCP tool calls. Provider mutation remained blocked in the PyPI pass because the installed version saw shared/global GrepAI state. Repo-local `ar-memory/` remains a confirmed MCP resolver-parity defect.

Provider compatibility follow-up 01 added source-level Docker integration coverage for worktree and benchmark provider setup. The final integration pass exercised a source provider set, a worktree-local provider set, and a benchmark-local provider set for both GrepAI and CodeGraphContext, including GrepAI DB clone, CGC export/rewrite/import seeding, watcher status, and test cleanup. The remaining parent-provider items still need a fresh PyPI MCP validation pass after these source changes are packaged/installed.
